pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '🔧 Build stage started'
            }
        }

        stage('Done') {
            steps {
                echo '✅ Build completed successfully!'
            }
        }
    }
}
